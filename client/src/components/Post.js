import React, { useState } from "react";
// import { Link } from "react-router-dom";

const api = "/recipes";

const Post = ({ posts, loadPosts }) => {
    return (
        <div>
        </div>
    );
};

export default Post;
